apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-reader
  namespace: dev
rules:
  - apiGroups: ["","apps"]
    resources: ["pods","daemonsets"]
    verbs:
      - get
      - list
      - watch 