apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-dep
  annotations:
    kubernetes.io/change-cause: "v-2.0"
spec:
  replicas: 3
  selector:
    matchLabels:
      env: prod
  template:
    metadata:
      name: mydeply
      labels:
        env: testing
    spec:
      containers:
        - name: my-cont
          image: nginx
          ports:
            - containerPort: 80